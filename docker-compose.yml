# Use the extended volume syntax so Coolify can manage directory creation.
volumes:
  - type: bind
    source: ./src
    target: /app/src
    is_directory: true

# If you still want these ports to be directly exposed on the host, keep them listed here.
ports:
  - '3000:3000'
  - '24678:24678'
