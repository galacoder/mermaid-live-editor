version: "3.7"

services:
  mermaid:
    image: ghcr.io/mermaid-js/mermaid-live-editor
    platform: linux/amd64

    # No "ports:" section here, so no direct host port binding
    environment:
      - SERVICE_FQDN_MERMAID_8080
